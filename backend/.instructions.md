---
applyTo: "backend/**"
---

# Backend — Copilot Instructions

You are working in the **backend** of Zault, a Java 25 + Spring Boot application.

## Key Context

- Read `CONTEXT.md` (root) for system architecture.
- Read `backend/CONTEXT.md` for backend-specific decisions and state.
- Read `docs/api/openapi.yaml` for the current API contract.

## Schema Changes

Whenever a JPA entity or `UserDatabaseService.initializeSchema()` is added or modified:
- **Main DB tables** → update `src/main/resources/schema.sql` AND the Data Model section in `backend/CONTEXT.md`
- **Per-user DB tables** → update `UserDatabaseService.initializeSchema()` AND the Data Model section in `backend/CONTEXT.md`

The Data Model section in `CONTEXT.md` is the single source of truth for human-readable schema documentation — keep it in sync on every schema change.

## Do NOT

- **Do NOT read frontend code.** The API contract in `docs/api/openapi.yaml` is the boundary.
- Do NOT suggest Gradle — this project uses Maven with `./mvnw`.

## Conventions

- Package root: `dev.zault`
- **Packaging principle:** Domain packages for business features, shared infrastructure packages for cross-cutting concerns.
- REST endpoints under `/api/` prefix
- Database: SQLite (JPA + Hibernate community dialect)
- Production runtime: GraalVM native image
- CLI commands: use `make` targets (see root `Makefile`)
- Environment variables documented in `.env.example` — never hardcode secrets
- **Monetary amounts:** Always round to 4 decimal places at the service/validation boundary. Quantities may carry fractional share precision where applicable. Reject or round values with more than 4 decimal places before persisting.

## API Spec

The OpenAPI spec is auto-generated by springdoc-openapi from controller annotations.
After changing any controller or DTO, use the `sync-api-spec` skill to regenerate `docs/api/openapi.yaml` and `frontend/src/api/schema.d.ts`. This is mandatory — do not skip it.

## Environment

```bash
export JAVA_HOME="/Users/viv/Library/Java/JavaVirtualMachines/corretto-25.0.2/Contents/Home"
```