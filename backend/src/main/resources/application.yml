spring:
  application:
    name: zault

  datasource:
    url: jdbc:sqlite:${SQLITE_PATH:./data/zault.db}?busy_timeout=${SQLITE_BUSY_TIMEOUT_MS:5000}
    driver-class-name: org.sqlite.JDBC
    hikari:
      maximum-pool-size: ${SQLITE_MAIN_DB_MAX_POOL_SIZE:8}
      minimum-idle: 1

  sql:
    init:
      mode: always

  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: none
    show-sql: false

server:
  port: 8080

management:
  endpoints:
    web:
      base-path: /api
      exposure:
        include: health, metrics, prometheus
  metrics:
    tags:
      application: ${spring.application.name}

zault:
  jwt:
    secret: ${JWT_SECRET:change-me-to-a-secure-32-char-min-secret!}
    expiry: ${JWT_EXPIRY:PT1H}
  admin:
    username: ${ZAULT_ADMIN_USERNAME:admin}
    password: ${ZAULT_ADMIN_PASSWORD:}
  security:
    max-failed-attempts: 5
    lockout-minutes: 15

  user-db:
    base-dir: ${ZAULT_USER_DB_DIR:./data/users}
    max-open-connections: ${ZAULT_USER_DB_MAX_OPEN:32}
    busy-timeout-ms: ${ZAULT_USER_DB_BUSY_TIMEOUT_MS:5000}

